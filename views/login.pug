extends layouts/main

block content
    .form-container
        form(action=routes.login, metion="post")
            input(type="email", name="email", placeholder="Email")
            input(type="password",name="password", placeholder="Password")            
            input(type="submit", value="Log In")